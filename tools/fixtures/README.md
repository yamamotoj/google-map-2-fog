## fixtures（テスト用Takeoutサンプル）

このリポジトリには、個人情報を含み得る Google Takeout の実データはコミットしません。

### ローカルテストで使う方法

- `tests/unit` は、最小限の JSON 断片（匿名化/ダミー座標）で動作確認します
- 必要なら、Takeoutから抜き出した小さな断片を **緯度経度と時刻をぼかした上で** 自分のローカルにのみ配置してください


